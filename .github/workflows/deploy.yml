name: Deploy to Home Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, home]
    steps:
      - name: Run update script
        run: bash /home/murad/secretary-ai/deploy/update.sh
